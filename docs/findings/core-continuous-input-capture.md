---
status: active
created: 2026-09-06
scope: controller-input-lifetimes
remove_when: a matched API 47 shell/core has routed live evidence for the integrated lifecycle boundary
---

# Interaction lifecycle live validation

The shared cancellation manager, declared edge/motion companions, exact target fences and old-path
removal are implemented. The [permanent contract](../interaction-lifecycle.md) records architecture,
offline test results and remaining host constraints. No continuation shim remains in this scope.

The implementation gap is closed; the required live evidence is pending. Under one singleton lease,
install the matched build and verify these real routed cases against host read-back and Push output:

- Hold/touch through core-page replacement and entry into an unmigrated page: cleanup once, no
  leaked motion/release, fresh press usable.
- Rebind a project remote/send or switch project/selected track while holding its control: no
  replacement-target mutation and later exact lease retirement.
- Exercise pad pressure, raw bend and fill cancellation, including host-delayed fill retirement.

The installed Bitwig build was left untouched during integration. Remove this finding after recording
exact-build live evidence; offline fakes alone do not prove Bitwig runtime proxy/callback enforcement.
