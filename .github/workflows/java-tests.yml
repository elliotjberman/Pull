name: Java tests

on:
  pull_request:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: Maven tests (Java 21)
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Test Push debugger clients
        run: tools/test-push-debug-clients

      - name: Test Pull live lease
        run: tools/test-pull-live-lock

      - name: Test Push debugger surface server
        run: python3 -m unittest tools/test_push_debug_surface_server.py

      - name: Set up Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: "**/pom.xml"

      - name: Run unit tests
        run: mvn --batch-mode --no-transfer-progress test
